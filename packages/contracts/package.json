{
  "name": "bountychain-contracts",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@openzeppelin/contracts": "5.0.0",
    "hardhat": "^2.22.19"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "grep": "bun run test --grep",
    "hd": "hardhat"
  }
}
